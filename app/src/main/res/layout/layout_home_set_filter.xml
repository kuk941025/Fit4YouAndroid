<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/layout_home_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/margin_side"
    android:layout_marginBottom="@dimen/margin_side"

    >

    <TextView
        android:id="@+id/layout_home_selected_filter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:text="Selected Filter"
        app:layout_constraintBottom_toBottomOf="@+id/layout_home_btn_set_filter"
        app:layout_constraintEnd_toStartOf="@+id/layout_home_btn_set_filter"
        app:layout_constraintTop_toTopOf="@+id/layout_home_btn_set_filter" />

    <Button
        android:id="@+id/layout_home_btn_set_filter"
        android:layout_width="35dp"
        android:layout_height="30dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="@dimen/margin_side"
        android:background="@color/colorPrimaryDark"
        android:text="+"
        android:textColor="@color/colorWhite"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</android.support.constraint.ConstraintLayout>